# 微信分身 bot 运维手册(脱敏公开版)

> 这是踩了很多坑总结出的运维经验。真名/真实事故已抽象成通用教训。

## ⚠️ 铁律
1. **bot 运行时别手动碰那台手机**——会和 bot 抢控制打架。诊断只读日志。
2. **要进手机查问题必须先停 bot**(`launchctl unload`),否则一进对话就把未读标已读,可能吞掉别人的 @。
3. 截图坐标用原生分辨率;换机器/换微信版本要重标。

## 常驻托管(launchd)
- 用 launchd 常驻(RunAtLoad + KeepAlive),挂了自动拉回。
- ⚠️ launchd 的 PATH 是最小化的,plist 里要显式配 `PATH` 含 adb 所在目录,否则 `FileNotFoundError: adb` 启动即崩。
- 别用 `pkill`——KeepAlive 会立刻拉回;要停必须 `unload`。

## 连接:WiFi ADB
- USB 隔夜易掉线(有些 ROM 半夜切 USB),改用 WiFi ADB 更稳,USB 只充电。
- 手机换 IP 会断——靠 mDNS 自动发现自愈。⚠️ **两种广播都要认**:`_adb-tls-connect._tcp`(无线调试开关,端口动态)和 `_adb._tcp`(`adb tcpip` 模式,端口 5555)。只认一种会在某些换 IP 场景下自愈失败。
- 手机设「睡眠时保持 WLAN=始终」;最好路由器绑固定 IP。
- 排查:断连时先 `adb mdns services`,列出手机就是发现逻辑的锅,不是手机的锅。

## 中文输入 / 发送
- adb 原生只能打 ASCII;中文走 ADBKeyboard 的 broadcast(`ADB_INPUT_TEXT`)。
- 每次发送前强制切回 ADBKeyboard——系统/断连会把输入法重置回默认,导致打不进字。
- 发送键别写死坐标,扫微信绿求质心(肉眼估会偏)。
- ⚠️ **"点了"≠"发出去了"**:文字换行后输入框变高、发送键上移,可能点空。点完要**回读验证**(输入框空了=发出去了),失败重定位重试。会话列表上的 `[草稿]` 就是没发出去的铁证。

## 看屏决策的坑
- **会话名 OCR 变体**会把记忆切碎(同一个群被认成好几个名字,各存一份)。解法:相似度归并 key + 定期用 `merge_keys.py` 体检。
- **点错行**:读列表要几十秒,期间来新消息列表会整体移位→点到隔壁。三道防线:①让模型同时给行号 ②半行以上偏移直接跳过 ③点前用目标行头像哈希复核,变了就重扫。
- **进错会话后遗症**最狠:上下文喂错→模型连自己报的会话名都跟着错→说错话还写进错档案。正解:**进门先认门牌**(标题栏像素哈希),对不上就不加载上下文、不决策、不说话。
- 图片消息的去重指纹会撞车(文本都是 `[图片]`)→ 一个群所有图判成"已处理"不回。解法:占位符消息补一段画面哈希。

## 记忆设计(定过的调子)
- **两层**:对话层(聊过啥,自动)+ 人物层(是谁,人工维护)。**人物身份绝不自动写**——错事实会永久污染回复。
- **梗/事实里带人名 = 伪装成情节的身份判断**,一样有污染风险。自动写入只适合不含主体的纯事件,否则走"问机主"。
- **真实语料 > 手写规则 > 模型自己的话**。想改口吻先改样本;别拿"分身模仿你的话"回去训,会越学越像 AI。

## 告警/健康(血泪)
- **"日志在动"≠"分身活着"**:adb 掉线时日志一直刷报错,拿日志 mtime 当判据会一路报"正常"。必须用**心跳**(跑完一轮且 adb 通才写),看门狗看心跳。
- **误报比漏报更伤**——报几次就没人信了。断连要持续够久(如 15min)才算真掉;WiFi 抖一下不报。
- 告警**停摆期间要重播**(每 6h),只报一次那条被漏看就没了。
- 熔断:某会话连续发失败到阈值就冷却 + 告警,别无限重试。

## 改代码后必跑
```bash
python3 -m pyflakes wechat_autoreply.py
python3 -m pylint --disable=all --enable=E0601,E0602 --score=n wechat_autoreply.py  # E0601=先用后赋,曾害过一次17小时静默
```
